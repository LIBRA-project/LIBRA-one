name: libra-one-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - openmc
  - ipykernel
  - nbconvert
  - vtk
  - pip
  - pip:
    - libra-toolbox>=0.6
    - openmc_data_downloader